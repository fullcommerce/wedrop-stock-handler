"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
/**
 * Create an Error with the specified message, config, error code and status.
 *
 * @param message The error message.
 * @param status The error status.
 * @param data The error data.
 * @param [code] The error code (for example, 'E_CONN_ABORTED').
 * @returns The created error.
 */
function createError(message, status, data, code) {
    const rawError = new Error(message);
    const error = Object.assign(Object.assign({}, rawError), { message,
        status,
        data,
        code, toJSON: () => {
            return {
                message,
                name: rawError.name,
                stack: rawError.stack,
                data,
                code
            };
        } });
    return error;
}
exports.default = createError;
